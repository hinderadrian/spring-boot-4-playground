spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/playground
      client:
        provider:
          playground:
            issuer-uri: http://localhost:8080/realms/playground
