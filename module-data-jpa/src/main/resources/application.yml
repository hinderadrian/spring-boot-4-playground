spring:
  application:
    name: module-data-jpa
  datasource:
    url: jdbc:postgresql://localhost:5432/playground
    username: playground
    password: playground
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
